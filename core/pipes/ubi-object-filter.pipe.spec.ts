import { UbiObjectFilterPipe } from './ubi-object-filter.pipe';

describe('UbiObjectFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UbiObjectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
