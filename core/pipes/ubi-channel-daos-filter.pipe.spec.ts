import { UbiChannelDaosFilterPipe } from './ubi-channel-daos-filter.pipe';

describe('UbiChannelsDaoFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new UbiChannelDaosFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
