import { UbiFieldValueDirective } from './ubi-field-value.directive';

describe('UbiFieldValueDirective', () => {
  it('should create an instance', () => {
    const directive = new UbiFieldValueDirective();
    expect(directive).toBeTruthy();
  });
});
