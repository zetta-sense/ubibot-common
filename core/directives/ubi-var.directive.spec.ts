import { UbiVarDirective } from './ubi-var.directive';

describe('UbiVarDirective', () => {
  it('should create an instance', () => {
    const directive = new UbiVarDirective();
    expect(directive).toBeTruthy();
  });
});
