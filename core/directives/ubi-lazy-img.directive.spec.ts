import { UbiLazyImgDirective } from './ubi-lazy-img.directive';

describe('UbiLazyImgDirective', () => {
  it('should create an instance', () => {
    const directive = new UbiLazyImgDirective();
    expect(directive).toBeTruthy();
  });
});
